# --- Secrets ---
# Ignore SSH keys generated locally. These should not be committed.
ssh/
# Or be more specific if needed:
# ssh/id_rsa

# --- OS / Editor / IDE Files ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- Build / Dependency Artifacts ---
# If Hadoop tarball downloaded locally:
hadoop-*.tar.gz

# --- Python ---
# Ignore virtual environments
venv/
.venv/
env/
# Ignore caches and compiled files
__pycache__/
*.pyc
*.pyo

# --- Temporary Files ---
# Ignore specific temp files if generated by scripts
sample-wordcount-input.txt
*.tmp

# --- Logs ---
# Ignore local log files if any are generated (unlikely here)
*.log

# --- Docker ---
# Docker volumes are managed by Docker, but ignore if mapped locally
# e.g. namenode-data/
# e.g. datanode-data/